# MT-tgt FEW-SHOT 场景数据集配置 (Bias Finetune 实验)
# 直接在目标域 few-shot 数据上做 YOLO + L_bias 微调

train: /root/autodl-tmp/dataset/MT-tgt-split/MT-tgt_few-shot_train/images
val: /root/autodl-tmp/dataset/MT-tgt-split/MT-tgt_few-shot_val/images
test: /root/autodl-tmp/dataset/MT-tgt-split/MT-tgt_global_test/images

nc: 6
names: ['chamfer', 'multifaceted', 'bump', 'impurity', 'crack', 'grind']

# 场景信息
scenario: few-shot
description: "Bias-Controlled YOLO training on MT-tgt few-shot scenario (1% data)"
experiment_type: "yolo_bias_finetune"
