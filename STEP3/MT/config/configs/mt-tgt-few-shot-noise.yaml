# MT-tgt FEW-SHOT 场景带噪声的 train 配置
#
# 说明：
# - train 使用 MT-tgt-split-noise/MT-tgt_few-shot_train_mix（clean + D_noise + OOD）
# - val/test 保持与原 few-shot 场景一致，用于公平对比。

train: /root/autodl-tmp/dataset/MT-tgt-split-noise/MT-tgt_few-shot_train_mix/images
val: /root/autodl-tmp/dataset/MT-tgt-split/MT-tgt_few-shot_val/images
test: /root/autodl-tmp/dataset/MT-tgt-split/MT-tgt_global_test/images

nc: 6
names: ['chamfer', 'multifaceted', 'bump', 'impurity', 'crack', 'grind']

scenario: few-shot-noise
description: "MT-tgt few-shot 带显式噪声集 (D_noise + OOD) 的 train 配置"
experiment_type: "yolo_bias_finetune_noise"

