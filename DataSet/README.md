# DataSet (本地只读数据)

此目录用于放置**原始数据**（只读）。它是本项目的本地数据入口，但**不随 GitHub 仓库分发**（默认被 `.gitignore` 忽略）。

建议约定：
- `DataSet/` 只放原始数据或下载后的数据快照（不要在这里做清洗/切分）。
- 任何预处理产物请放到 `Data-pre/`。

常见做法：把真实数据存放在其它磁盘路径，然后在仓库根目录创建软链接：

```bash
ln -s /path/to/your/DataSet DataSet
```
